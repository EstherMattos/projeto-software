// DUMMY

/*Pagamento_controle é responsável por finalizar a compra e redirecionar para a API
de compras do stripe.
*/

const Pagamento_controle = () => {

    //Faz o Pagamento
    const pagamento = (event) => {}

}