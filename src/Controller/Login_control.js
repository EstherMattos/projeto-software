// DUMMY

/*Login_controle é responsável por logar, deslogar e cadastrar um usuário no sistema,
ele chama autenticacao_google para autenticar os dados e gerar um token, e 
usuario_model e admin_model para checar se os dados autenticados existem no DB e qual
o tipo de usuario que esta logando.
*/

const Login_controle = () => {

    //Faz o login
    const logar = (event) => {}

    //Faz logout
    const sair = (event) => {}

    //Cadastra novo usuario
    const cadastrar = (event) => {}


    //VIEW
    return (
        <div>
            <p>Pagina de perfil do usuario</p>
        </div>
    )

}

export default Login_controle;