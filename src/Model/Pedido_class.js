// DUMMY

/*Essa classe representa a tabela categoria do DB, e o objeto categoria pode
ser usado pelas demais classes e passado como parametro.
*/

class Categoria {

    constructor(id, nome) {
        this.nome = nome;
        this.id = id;
    }

}