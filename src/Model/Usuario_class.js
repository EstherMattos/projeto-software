// DUMMY

/*Essa classe representa a tabela usuarios do DB, e o objeto usuario pode
ser usado pelas demais classes e passado como parametro.
*/

class Usuario {

    constructor(id, nome, email) {
        this.email = email;
        this.nome = nome;
        this.id = id;
    }

}